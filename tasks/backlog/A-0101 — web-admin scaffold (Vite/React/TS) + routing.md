# A-0101 — web-admin scaffold (Vite/React/TS) + routing


## Goal
관리자 웹 기본 뼈대 생성.

## Scope
- Vite + React + TS
- router: login, dashboard, jobs, audit, workspace/members
- dev port 5173
- shared API client pattern with web-user (packages/api-client optional)

## Acceptance Criteria
- `pnpm -C web-admin dev --port 5173` 실행 가능

