# U-0101 — web-user scaffold (Vite/React/TS) + routing


## Goal
사용자 웹 기본 뼈대 생성.

## Scope
- Vite + React + TS
- Router 설정 (login, workspace, inbox, doc detail, tree)
- dev port 5174
- API client wrapper(axios/fetch) + request_id header

## Acceptance Criteria
- `pnpm -C web-user dev --port 5174` 실행
- placeholder pages 렌더링

