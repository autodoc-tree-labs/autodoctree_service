# A-0115 — Admin E2E smoke (Playwright) + CI


## Goal
관리자 핵심 플로우 E2E 검증.

## Scope
- Playwright:
  - login → select workspace → jobs 검색 → retry(스텁 가능)
  - audit 조회
- CI 연결(I-0103)

## Acceptance Criteria
- PR에서 최소 smoke 통과

