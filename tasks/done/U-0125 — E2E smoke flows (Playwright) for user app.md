# U-0125 — E2E smoke flows (Playwright) for user app


## Goal
사용자 핵심 플로우를 E2E로 검증.

## Scope
- Playwright:
  - login → workspace select → create doc → list 확인
  - upload(스텁 가능) → status 확인
- CI 연결(I-0103)

## Acceptance Criteria
- PR에서 최소 smoke 통과

